# print-system

This repository contains the reporting system proof-of-concept that replaces Crystal-style report flow with a web/HTML-based, compile-first architecture.

- `ReportPoc/` holds the runnable ASP.NET Core prototype.
- `PROJECT-HANDOFF.md` contains full continuation context, runbook, flow and GitHub steps.
- root `.md` files document the architecture decisions and alternatives considered.
